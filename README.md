# FE_ScholarTrend

ScholarTrend frontend built with React 18 + Vite.

## Setup

```bash
npm install
npm run dev
```

Create a `.env` file from `.env.example` to configure API base URL:

```bash
VITE_API_BASE_URL=http://localhost:8080/api
```
